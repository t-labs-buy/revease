import { ComingSoon } from "@/components/ui";
export default function Page() {
  return (
    <ComingSoon
      title="Ask AI"
      desc="Ask questions across all your captures and get cited answers, sourced to specific workflow steps."
      track="Track H — Ask AI & Knowledge Base (RAG)"
    />
  );
}
