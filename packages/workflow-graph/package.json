{
  "name": "@refract/workflow-graph",
  "version": "0.1.0",
  "description": "Refract Workflow Graph IR — JSON Schema + TypeScript validator (single source of truth).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./schema/workflow-graph.schema.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/index.test.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
